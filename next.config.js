// const { i18n } = require('./next-i18next.config')

module.exports = {
  // i18n,
  output: 'export',
  
  distDir: 'dist',

  images: {
    unoptimized: true,
  },

  reactStrictMode: true,

  // trailingSlash: true,
}
